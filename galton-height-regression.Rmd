---
title: "galton-height-regression"
output: html_document
---

# Predicting Child Height from Parental Height: A Linear Regression Analysis of the Galton Dataset

## Summary
Height is known to have a strong genetic component, and parental height is often used as a predictor of child height. In this analysis, we examined the relationship between midparent height (the average height of both parents) and child height using the Galton height dataset. A simple linear regression model was fitted to evaluate how well midparent height predicts child height. The results indicated a significant positive relationship between the two variables, with midparent height explaining approximately 10.3% of the variance in child height. These findings suggest that while parental height is an important factor, other genetic and environmental factors also contribute to a child's height.

## Introduction
Human height is influenced by a combination of genetic and environmental factors, but parental height is often considered a strong predictor of a child's height . Early statistical work by Francis Galton demonstrated that the average height of both parents, known as midparent height, provides a strong indicator of a child’s expected height (Galton, 1886)Galton also observed that children’s heights tend to regress toward the population average rather than exactly matching their parents, a phenomenon later known as regression toward the mean. The Galton height dataset, collected in the late 19th century, contains measurements of parents’ heights and the heights of their children. The dataset includes a variable called midparent height, which represents the average height of both parents, and allow us to study how parental height relates to the height of their child. In this analysis, we aim to answer the question: **Can a child’s height be predicted based on their parents’ heights?** To address this question, we use a simple linear regression model to examine the relationship between midparent height and child height. By analyzing this relationship, we can better understand how parental height contributes to variation in child height.

## Methods

To investigate the relationship between parental height and child height, we conducted a simple linear regression analysis using the Galton height dataset. The dataset includes measurements of midparent height (the average height of both parents) and the height of their child.

A scatter plot was created using `ggplot` to visualize the relationship between midparent height and child height. Each point represents one parent–child pair. The fitted regression line and confidence interval were added to the scatter plot using `geom_smooth(method = "lm")`.

A linear regression model was fitted with child height as the response variable and midparent height as the predictor variable. This model estimates how much child height is expected to change with each one-inch increase in midparent height. Model coefficients and statistical significance were evaluated using the `summary()` function in R.

## Analysis

```{r}
library(tidyverse)

# Load data
galton_heights <- read_csv("data/processed/galton_clean.csv")

# Scatterplot with regression line
ggplot(galton_heights, aes(x = midparent_height, y = child_height)) +
  geom_point(alpha = 0.4) +
  geom_smooth(method = "lm", se = TRUE) +
  labs(
    x = "Midparent Height (inches)",
    y = "Child Height (inches)",
    title = "Relationship Between Parent and Child Height")

# Fit linear regression model
model <- lm(child_height ~ midparent_height, data = galton_heights)

# Model summary
summary(model)
```

## Results

The scatter plot shows a positive relationship between midparent height and child height. In general, children with taller parents tend to have taller heights.

The linear regression analysis indicated that midparent height was a significant predictor of child height (β = 0.64, p \< 0.001). This suggests that for every one-inch increase in midparent height, the child's height is predicted to increase by approximately 0.64 inches.

The overall regression model was statistically significant (p \< 0.001), explaining 10.3% of the variance in child height (R² = 0.103).

## Discussion

## References
Galton, F. (1886). Regression Towards Mediocrity in Hereditary Stature **Journal of the Anthropological Institute**, 15, 246-263